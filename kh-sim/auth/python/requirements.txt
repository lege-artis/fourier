# kh-sim/auth/python/requirements.txt
# AUTH-003 -- Keycloak OIDC client (Python)
#
# Install:
#   pip install -r kh-sim/auth/python/requirements.txt
#
# Runtime constraints:
#   Python >= 3.11  (project standard; see AUTH-PROVIDER-ADR.md)
#   authlib 1.x     Pinned to 1.x for stable jose / oauth2 API surface
#   httpx 0.27.x    Async HTTP client; aligns with FastAPI recommended transport

authlib>=1.3.0,<2.0.0
httpx>=0.27.0,<1.0.0

# Optional -- needed only if using the FastAPI middleware example in module docstring
# fastapi>=0.111.0
# starlette>=0.37.0
