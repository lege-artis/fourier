server {
    listen 8080;
    server_name kh-cpp.test;

    # Reverse proxy to C++ backend (cpp-httplib, port 8003)
    location /api/ {
        proxy_pass         http://127.0.0.1:8003/;
        proxy_http_version 1.1;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_read_timeout 120s;
    }

    location / {
        proxy_pass         http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
    }

    location = /health {
        proxy_pass http://127.0.0.1:8003/health;
    }

    access_log  D:/Apps/Laragon/logs/kh-cpp-access.log;
    error_log   D:/Apps/Laragon/logs/kh-cpp-error.log warn;
}
